# 從黑箱行為推論到後端安全約束修補：計畫初稿（修訂版 v1）

> 修訂日期：2026-09-09  
> 文件狀態：研究架構工作稿，後續仍須依徵件格式、目標平台與實驗資源細化。  
> 原始規劃：[proposal-initial-plan-gemini.md](./proposal-initial-plan-gemini.md)

## 一、計畫核心主軸

現代 Web 與行動應用的業務邏輯漏洞，經常不是由單一危險函式或典型 `Source → Sink` 資料流造成，而是後端未在正確位置執行必要的狀態、角色或資源所有權約束。例如，系統可能允許使用者跳過付款直接觸發出貨，或以自己的合法憑證操作其他使用者的資源。這類漏洞的輸入往往具有合法型態，僅靠傳統污點分析或單一請求掃描不易辨識。

本計畫聚焦以下核心科學問題：

> 給定成對的合法／違規業務軌跡及後端程式碼，能否推導遭違反的安全不變量，將其定位至缺失或錯置的後端執行點，並生成能阻斷同類違規且保存合法工作流程的跨函式補丁？

為回答此問題，本計畫以「行為模型推論（Behavior Inference）→ 防禦缺失定位（Enforcement Localization）→ 約束引導修補（Constraint-Guided Repair）」為三年主線，並以「帶有對照證據的業務安全模型」作為跨年度核心中介資產。

三年的研究角色分別為：

1. **第一年——探測與建模：** 從 GUI 操作及其實際觸發的 API 流量建立業務狀態模型，透過跨狀態、跨角色與跨資源擁有者的對照測試，產生可重現的違規軌跡。
2. **第二年——診斷與定位：** 將合法／違規軌跡及其安全不變量對齊後端程式碼屬性圖，縮小缺失或錯置安全檢查的候選位置。
3. **第三年——修補與驗證：** 依據安全不變量、程式依賴與候選執行位置，規劃跨函式、跨檔案補丁，並同時驗證攻擊阻斷、同類攻擊泛化及合法功能保存。

## 二、研究範圍與操作型定義

### 2.1 主要漏洞範圍

本計畫集中處理兩類具有共同「後端安全約束缺失」本質的業務邏輯漏洞：

- **業務狀態轉移違規：** 跳步、重播、重新排序或繞過必要前置狀態，例如未付款即出貨。
- **授權與資源所有權違規：** 在相同操作序列中替換角色、帳號、憑證上下文或物件識別碼，使使用者越權操作他人資源，例如 IDOR／BOLA 類問題。

競爭條件、開放重新導向及一般輸入注入漏洞不列為主要研究標的，避免分散狀態與授權不變量的研究主線；若在探索過程中發現，僅作伴隨觀察紀錄。

### 2.2 關鍵名詞

- **業務狀態（Business State）：** 由穩定且與安全決策相關的 UI、API 回應及持久化結果共同描述的抽象狀態，而非單純將每一個畫面視為不同狀態。
- **合法軌跡（Valid Workflow Trace）：** 在特定角色、資源所有權及前置狀態下，能完成預期業務目標的 GUI 操作與 API 序列。
- **對照軌跡（Counterfactual Trace）：** 僅改變角色、資源、前置狀態或步驟順序等單一關鍵條件，用於比較系統行為差異的軌跡。
- **違規軌跡（Exploit Trace）：** 經可重現驗證，證實系統在不滿足必要條件時仍完成敏感操作或狀態轉移的對照軌跡。
- **安全不變量（Security Invariant）：** 敏感操作成立前必須滿足的狀態、角色或資源關係，例如 `order.status == PAID` 或 `resource.owner == authenticatedUser`。
- **執行位置（Enforcement Point）：** 後端應檢查安全不變量的程式位置，可能位於 Middleware、Controller、Service 或 Domain 層。

### 2.3 第一年黑箱邊界

第一年不存取前端、App 或後端原始碼，不進行反編譯、JavaScript Bundle 靜態分析、Bytecode 分析、二進位修改或 Client Instrumentation。可使用的觀察來源僅限：

- 一般使用者可觀察的 GUI 畫面。
- Runtime UI Automation／Accessibility Tree。
- 由實際 GUI 操作觸發且在受控測試環境中可觀測的網路請求與回應。
- 操作後可由 GUI 或公開 API 再次確認的持久化狀態。

網路流量可觀測性屬於實驗環境假設。若受測系統使用無法在不修改客戶端的前提下觀察之憑證綁定、應用層加密或自訂協定，則列為資料集排除條件或另行報告可觀測性限制，不將繞過此限制本身視為本計畫貢獻。

## 三、跨年度核心資產與推論鏈

本計畫不假設「觀察到的行為即為合法規範」。安全不變量須由合法軌跡與受控擾動後的對照證據共同推導，必要時允許以少量人工規則作為評估用的真值或種子規格。

核心推論鏈如下：

```text
GUI 與可觀測 API 行為
        ↓
安全相關狀態抽象與合法 Workflow
        ↓
跨狀態／跨角色／跨資源的對照測試
        ↓
可重現 Exploit Trace + 候選 Security Invariant
        ↓
後端 Route、變數、Entity 與 CPG 對齊
        ↓
Missing／Incorrect Enforcement Point 候選排序
        ↓
Constraint-Guided Multi-Hunk Patch
        ↓
攻擊阻斷 + 同類變異 + 合法流程保存驗證
```

每年度必須保留資料來源、推論信心及驗證結果，使前一年度的錯誤不會被無條件當成下一年度的正確輸入。

## 四、第一年：黑箱 GUI 探索、行為建模與違規軌跡生成

### 4.1 研究問題

> RQ1：僅使用 GUI 互動與其觸發之可觀測 API 流量，能否建立安全相關的業務狀態模型，並透過對照式流程擾動有效產生可重現的業務狀態與授權違規軌跡？

### 4.2 研究方法

#### A. 語意感知的 GUI 目標導向探索

- 結合 Accessibility Tree、OCR 與版面關係辨識可操作元件、功能模組及使用者意圖。
- 以可達成的業務目標驅動探索，例如建立訂單、付款、取消或查閱特定資源，而非進行無目的隨機點擊。
- 將動畫、時間戳、廣告與非安全相關視覺變化正規化，避免狀態數量爆炸。

#### B. 安全相關狀態與工作流程推論

- 將 GUI 狀態、API 序列、角色、資源識別及操作後持久化結果整合為狀態轉移模型。
- 辨識完成敏感操作前反覆出現的前置狀態與關係，形成候選安全不變量。
- 為每條轉移保留可觀測證據與信心分數，不把單次成功行為直接視為合法規範。

#### C. 對照式工作流程擾動

針對已取得的合法軌跡進行最小差異變異：

- 省略、重播或重新排序流程步驟。
- 在不同前置狀態下重送相同操作。
- 替換登入角色、帳號、憑證上下文或資源識別碼。
- 維持其他條件不變，比較 API 回應、GUI 呈現及操作後持久化狀態。

#### D. 攻擊成功判定

不以 HTTP 200、單一回應文字或畫面差異直接判定漏洞。違規軌跡至少須證明：

1. 敏感操作或狀態轉移確實發生。
2. 必要前置狀態、角色或所有權條件未成立。
3. 結果可由重新查詢、另一帳號觀察或重跑軌跡穩定重現。

自動化系統負責產生候選與證據；實驗資料集中的漏洞真值則以既有規格、人工確認或已知修補紀錄建立，避免用系統自己的判定結果評估自己。

### 4.3 評估指標

- 安全相關狀態與轉移的 Precision、Recall 或模型相似度。
- 已知合法 Workflow 的覆蓋率與完成率。
- 已知漏洞的發現率、誤報率及可重現率。
- 產生首條有效違規軌跡所需的操作次數與時間。
- 相較隨機探索、無語意模型探索及不使用對照策略方法的效益。
- 移除 GUI 語意、API 證據、持久化確認或對照策略後的消融結果。

### 4.4 年度產出

- 帶有角色、資源、前置條件及證據的業務安全狀態模型。
- 合法軌跡、對照軌跡與可重現 Exploit Trace。
- GUI 操作腳本、API 序列、驗證 Oracle 及候選安全不變量。
- 第一年度黑箱業務邏輯測試原型。

## 五、第二年：Trace-to-CPG 對齊與缺失安全約束定位

### 5.1 研究問題

> RQ2：給定合法／違規對照軌跡與後端原始碼，能否推導遭違反的安全不變量，並有效縮小 Missing／Incorrect Enforcement Predicate 的候選程式位置？

### 5.2 研究邊界

第二年進入白箱分析，但標的嚴格限制於後端 API 伺服器端，包括 Route、Middleware、Controller、Service、Domain Model 與 DAO／ORM。前端與 App 僅為第一年軌跡的流量發起端，不納入白箱程式碼分析。

### 5.3 研究方法

#### A. 後端專案級程式碼屬性圖

- 建立融合 AST、CFG、控制／資料依賴及跨函式呼叫關係的 CPG。
- 納入 Web Framework Route、Middleware Chain、Controller Binding 及 ORM Entity 關係。
- 連結 API 端點、請求欄位、身分上下文、Domain Object 與敏感資料操作。

#### B. Trace-to-Code 分層對齊

依序處理：

1. HTTP Method／Route 至 Endpoint Handler。
2. Request 欄位及認證上下文至程式變數。
3. 外部 Object ID 至 Domain／ORM Entity。
4. Handler 至敏感狀態更新或資料操作的跨函式路徑。

當一條外部軌跡可能對應多條內部路徑時，系統輸出排序後的候選，而不預設能無歧義地找出唯一執行路徑。

#### C. 安全不變量具體化

- 比較合法與違規軌跡中唯一改變的狀態、角色或資源關係。
- 將抽象條件映射為程式中的狀態欄位、認證主體、資源擁有者或角色變數。
- 產生可供程式分析與修補使用的候選 Predicate，例如狀態前置條件或主體—資源關係。

#### D. Missing／Incorrect Enforcement Analysis

- 以 Dominator、控制依賴及資料依賴分析檢查敏感操作前是否存在必要 Predicate。
- 區分完全缺少檢查、檢查使用錯誤變數、檢查位置過晚及僅在部分入口執行等情形。
- 依跨入口覆蓋範圍及職責，判斷安全檢查較適合位於 Middleware、Controller 或 Service／Domain 層。
- 使用 Call Fan-in、動態／軌跡覆蓋頻率、敏感操作可達性及權限影響範圍，建立候選修復位置的風險與優先級排序。

### 5.4 評估指標

- 漏洞根因的 File、Function 及 Statement／Predicate Top-k 定位準確率。
- 候選安全不變量與人工真值的一致程度。
- 相較純靜態分析、純動態軌跡、純文字檢索及不使用對照軌跡方法的定位改善。
- 程式切片縮減比例、分析時間及跨函式深度。
- 分別移除 CPG、動態證據、對照軌跡及安全不變量映射的消融結果。
- 第一年度錯誤輸入對第二年度定位表現的敏感度分析。

### 5.5 年度產出

- 後端 Trace-to-CPG 對齊與 Missing-Guard Analysis 原型。
- 漏洞根因定位報告，包含證據軌跡、切片、候選檔案／函式及缺失 Predicate。
- 需修復函式與執行位置的多維度風險矩陣。
- 可由第三年度修補系統直接使用的安全不變量與程式依賴表示。

## 六、第三年：約束引導的 Multi-Hunk 修補與閉環驗證

### 6.1 研究問題

> RQ3：給定 Exploit Trace、安全不變量及候選執行位置，能否生成跨函式、跨檔案補丁，使原始及同類違規軌跡失效，同時保存已知與鄰近合法工作流程？

### 6.2 研究方法

#### A. 複合型業務邏輯修補資料集

資料集分為兩類：

- **真實漏洞集：** 蒐集具有可確認安全修補、可執行後端版本及可重現行為證據的開源案例，用於衡量外部有效性。
- **受控變異集：** 從真實業務程式中移除、錯置或弱化狀態與授權檢查，保留實際跨函式依賴，用於控制實驗及消融分析。

訓練／提示案例與測試案例須依專案切分；另保留跨專案或跨框架測試集，避免相同修補模板同時出現在建構與評估資料中。

#### B. 約束引導的 Agentic 修補規劃

- 將安全不變量、候選 Enforcement Point、CPG 呼叫依賴及既有程式慣例共同輸入修補系統。
- 先規劃需修改的函式、檔案及資料傳遞，再生成 Multi-Hunk Patch。
- 支援在共用 Middleware、Endpoint Handler 及 Domain Logic 間選擇適當執行層級。
- 每個候選補丁須附帶「其如何實現安全不變量」的可檢查對應關係，不只輸出程式碼差異。

#### C. 三層閉環驗證

1. **安全有效性：** 原始 Exploit Trace 必須被阻斷，且敏感狀態或資料操作不得發生。
2. **同類攻擊泛化：** 變換受害資源、角色、前置狀態、步驟順序與等價 API 入口，確認修補並非只過度配適單一請求特徵。
3. **功能保存：** 第一年度合法 Workflow、專案既有測試，以及從合法軌跡生成的鄰近正常路徑均須成功。

驗證失敗時，系統將失敗證據回饋至定位或修補階段。不得僅以回傳 `403 Forbidden`、封鎖整個端點或通過既有測試宣稱修補正確。

### 6.3 評估指標

- 可編譯／可部署補丁比例。
- 原始攻擊阻斷率與同類變異攻擊阻斷率。
- 合法 Workflow 保存率、既有測試通過率及鄰近合法路徑成功率。
- 人工確認的語意正確補丁比例。
- 正確補丁所需嘗試次數、時間、修改檔案數與 Hunk 數。
- 與單點修補、未使用安全不變量、未使用 CPG 定位及直接提供完整專案給 LLM 等方法比較。
- 真實漏洞集與受控變異集分別報告，不以合成結果取代真實案例證據。

### 6.4 年度產出

- 約束引導的專案級 Multi-Hunk 後端修補系統。
- 真實與受控變異的業務邏輯修補評測資料集。
- 包含攻擊阻斷、泛化測試與功能保存證據的修補報告。
- 串聯「行為證據 → 安全不變量 → 執行位置 → 修補與驗證」的完整實證結果。

## 七、跨年度研究問題與產出總覽

| 維度 | 第一年：探測與建模 | 第二年：診斷與定位 | 第三年：修補與驗證 |
| --- | --- | --- | --- |
| 核心定位 | Behavior Inference | Enforcement Localization | Constraint-Guided Repair |
| 核心問題 | 能否由 GUI／API 對照軌跡建立安全相關模型並發現違規？ | 能否從對照軌跡推導不變量並縮小缺失檢查位置？ | 能否生成阻斷同類違規且保存合法流程的 Multi-Hunk 補丁？ |
| 程式碼邊界 | 純黑箱，不接觸任何原始碼 | 白箱，僅分析後端 API 原始碼 | 白箱，僅修改後端 API 原始碼 |
| 核心輸入 | GUI、Accessibility Tree、受控環境可觀測流量 | 合法／違規軌跡、安全不變量候選、後端程式碼 | 安全不變量、定位候選、程式依賴、合法／違規軌跡 |
| 核心方法 | 語意探索、狀態抽象、對照式流程擾動 | CPG、Trace-to-Code 對齊、Missing-Guard Analysis | Multi-Hunk 規劃、約束引導生成、三層驗證 |
| 核心產出 | 業務安全模型、合法／違規軌跡、PoC | 缺失 Predicate、程式切片、執行位置排序 | 修補 Agent、跨檔案補丁、閉環驗證證據 |

## 八、主要風險與因應方式

| 研究風險 | 可能影響 | 因應方式 |
| --- | --- | --- |
| 黑箱觀察不足以推導規範 | 將罕見合法流程誤判為漏洞 | 使用合法／違規對照、少量種子規則及獨立人工真值，不將單次觀察直接視為規範 |
| GUI 狀態數量爆炸 | 探索成本過高、模型不穩定 | 僅保留與角色、資源及敏感操作相關的穩定狀態特徵 |
| 網路流量不可觀測 | 無法建立 GUI—API 對應 | 明定受控環境假設與排除條件，獨立報告可觀測性限制 |
| 外部 Trace 與內部程式路徑存在歧義 | 無法定位唯一根因 | 輸出 Top-k 候選與信心，不預設精確唯一對齊 |
| 前一年誤差向後傳遞 | 定位及修補結果失真 | 保存證據與信心、分別以真值輸入及自動輸入評估誤差傳播 |
| 合成資料過於模板化 | 高估修補泛化能力 | 依專案切分資料，真實案例與受控變異分開報告 |
| 補丁以全面拒絕方式阻斷攻擊 | 安全通過但功能失效 | 同時執行合法 Workflow、鄰近正常路徑及既有測試 |

## 九、尚待定案項目

為使本計畫可以落地，後續需優先決定：

1. **目標系統型態：** Web、Android App，或兩者皆納入。
2. **後端技術範圍：** 建議先限定一至兩種具成熟解析工具的語言／Framework，避免 CPG 建模範圍失控。
3. **資料集來源：** 真實開源案例、既有測試平台、自建應用與受控變異各占多少比例。
4. **安全規格來源：** 完全自動推導、人工種子規則，或兩者混合的評估設定。
5. **自動化程度：** 系統是全自動判定漏洞，或自動產生候選與證據後由研究者確認。
6. **計畫徵件格式：** 頁數、年度經費、人力、預期論文／系統成果及審查領域。

上述項目確定後，才能進一步形成正式的研究動機、文獻缺口、工作項目、里程碑、資料集規模與量化驗收標準。
